@import '../stylus/base/**'

.main
  background #f5f5f5
  left 0
  margin 0 0 60px
  min-height calc(100vh - 236px)
  padding 0
  position relative
  top 60px
  right 0
  +tablet()
    min-height calc(100vh - 216px)
  &:after
    content ''
    display table
    clear both
